{
  "name": "getout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:decoders": "node scripts/sync-three-decoders.mjs",
    "predev": "npm run sync:decoders",
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "prebuild": "npm run sync:decoders",
    "build": "vite build",
    "check": "npm run typecheck && npm run lint && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.19.3",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/three": "^0.185.1",
    "eslint": "^10.6.0",
    "globals": "^17.7.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.63.0",
    "vite": "^8.1.4"
  }
}
